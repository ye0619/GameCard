# ============================================================
# GameCard 主配置文件
# ============================================================
spring:
  application:
    name: gamecard
  profiles:
    active: dev

# ============================================================
# AI 配置（预留）
# ============================================================
ai:
  enabled: false
  provider: openai-compatible
  base-url:
  api-key:
  model:
